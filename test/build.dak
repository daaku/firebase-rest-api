#!./node_modules/.bin/dak

(import [:esbuild :as esbuild]
        ["@daklang/esbuild" [dakPlugin]])

@(esbuild.build {:entryPoints ["test/tests.dak"]
                 :bundle true
                 :outfile "test/tests.mjs"
                 :sourcemap true
                 :plugins [(dakPlugin)]})
